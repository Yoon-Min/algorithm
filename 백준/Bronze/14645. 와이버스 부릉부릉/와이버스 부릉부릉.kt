import kotlin.math.*

fun main() {
    val (n,k) = readln().split(" ").map { it.toInt() }
    repeat(n) {
        val (a,b) = readln().split(" ").map { it.toInt() }
    }
    println("비와이")
}

